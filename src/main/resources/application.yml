spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/server_manager
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: false
server:
  error:
    include-message: always
